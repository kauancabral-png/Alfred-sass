{
  "name": "fincontrol-saas-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
     "backend",
     "frontend"
  ],
  "scripts": {
    "start": "npm start -w backend",
    "build": "npm run build -w backend"
  }
}
